# Session log

## Current state (2026-05-21)

### Completed this session
- Landing page at `/` (public route, redirects logged-in users to dashboard)
- Vercel email confirmed working (Resend + seagent.co.uk DNS pending order issue)
- Brief generation streaming — NDJSON events, sections render as Claude completes each
- Privacy policy at `/privacy` — covers Supabase, Vercel, Anthropic data processors
- Delete brief button (inline confirmation on brief page)
- Delete account (danger zone on dashboard, cascades all data)
- Brief page: per-section copy, copy-all, MEDDPICC per-element copy
- Case study checkboxes → controls which links append to email copy and mailto
- Send button (mailto with subject + body pre-filled)
- Strip markdown asterisks from Claude email output
- MEDDPICC section copy button labelled "Copy all"
- Per-case-study one-liner (generated by Claude, email-ready, stored on MatchedCaseStudy)
- File upload + drag-and-drop on new brief form (PDF, DOCX, TXT, multiple files)

### Outstanding
- Sample discovery calls (3x Apollo scenarios) — user generating in claude.ai, will bring back to wire in
- seagent.co.uk domain → Vercel connection (DNS not yet pointed to Vercel, only email DNS set up)
- Namesco email order issue to resolve
- Prompt caching on analyze route (cost optimisation, lower priority)

### Key technical notes
- pdf-parse must be pinned to v1.1.1 — v2 has completely different API (PDFParse class, URL-based)
- Import via `require("pdf-parse/lib/pdf-parse.js")` to avoid test-file side effect on init
- Regex in stripMarkdown must not use `s` flag — Vercel build target is below ES2018
- Next.js version is 16.2.6 (Turbopack) — middleware deprecated, should migrate to proxy
